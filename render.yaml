services:
  - type: web
    name: tiktok-bot
    env: python
    region: oregon
    plan: free
    branch: main
    buildCommand: |
      pip install -r requirements.txt
      playwright install
    startCommand: |
      python tiktik.py
